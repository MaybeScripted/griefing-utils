package griefingutils.modules;

public class BetterPauseScreen extends BetterModule {
    public BetterPauseScreen() {
        super(Categories.DEFAULT, "better-pause-screen", "Shows \"More\" button instead of \"Give Feedback\" and makes CTRL + C copy the server IP.");
    }
}
